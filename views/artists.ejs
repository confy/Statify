<main role="main">
      <section class="jumbotron text-center">
        <div class="container">
          <h1 class="jumbotron-heading">Your Top 50 Artists</h1>
          <p class="lead text-muted">This is where the words will go when they are written out! they will say something useful... Not too long and not too short...</p>
          <p>
            <a href="#" class="btn btn-primary my-2">Main call to action</a>
            <a href="#" class="btn btn-secondary my-2">Secondary action</a>
          </p>
        </div>
      </section>

      <div class="album py-5 bg-light">
        <div class="container">
            <div class="row">
            <% if (Object.keys(artists).length != 0 ) { %>
                <% Object.entries(artists).forEach(function (artist) { %>
                        <div class="col-md-4">
                          <div class="card mb-4 box-shadow">
                            <img class="card-img-top" src="<%= artist[1].images[0].url%>" alt="<%=artist[1].name %>">
                            <div class="card-body">
                              <p class="card-text"><b> <%= artist[1].name %></b></p>
                              <p class="card-text"> <%= artist[1].genres %></p>
                              <div class="d-flex justify-content-between align-items-center">
                                <div class="btn-group">
                                  <button type="button" class="btn btn-sm btn-outline-secondary"><a href="/artist/<%= artist[1].id %>">View</a></button>
                                </div>
                                <small class="text-muted">#<%=Number(artist[0]) + 1 %></small>
                              </div>
                            </div>
                          </div>
                        </div>
                      
                <% }) %>            
            <!-- Otherwise say no artists found -->
            <% } else { %>
                Your artists are empty!!!
            <% } %>
        </div>
        </div>
      </div>

    </main>

    <footer class="text-muted">
      <div class="container">
        <p class="float-right">
          <a href="#">Back to top</a>
        </p>
        <p>Album example is &copy; Bootstrap, but please download and customize it for yourself!</p>
        <p>New to Bootstrap? <a href="../../">Visit the homepage</a> or read our <a href="../../getting-started/">getting started guide</a>.</p>
      </div>
    </footer>
